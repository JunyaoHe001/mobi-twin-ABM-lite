# Public model inputs

This directory contains only disclosure-reduced synthetic inputs and simplified regional geometry for the Central Macedonia Live Lite demonstration. It contains no original sample rows, personal information, source identifiers, donor identifiers, source-row mappings, or private model files.

The browser uses the JSON and GeoJSON files directly. All outputs available from the interface are aggregate.
